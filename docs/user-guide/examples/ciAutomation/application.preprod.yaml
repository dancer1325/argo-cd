apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hello-world-preprod
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/dancer1325/argo-cd-hello-world-config
    targetRevision: HEAD
    path: helm
    helm:
      valueFiles:
      - values.preprod.yaml
  destination:
    # TODO: adjust it
    server: https://k8s-preprod.example.com
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
