data:
  # configure  -- based on the -- chosen Git provider's configured | step 1
  #   | GitHub
  webhook.github.secret: "<CONFIGURED_IN_STEP_1>"
  #   | GitLab
  webhook.gitlab.secret: "<CONFIGURED_IN_STEP_1>"
  #   | BitBucket
  webhook.bitbucket.uuid: "<CONFIGURED_IN_STEP_1>"
  #   | BitBucketServer
  webhook.bitbucketserver.secret: "<CONFIGURED_IN_STEP_1>"
  #   | Gogs
  webhook.gogs.secret: "<CONFIGURED_IN_STEP_1>"
  #   | Azure DevOps
  webhook.azuredevops.username: "<CONFIGURED_IN_STEP_1>"
  webhook.azuredevops.password: "<CONFIGURED_IN_STEP_1>"
